- name: Recyclarr info
  ansible.builtin.debug:
    msg: >-
      Recyclarr manages its own configuration via recyclarr.yml and runs on a
      schedule inside the container. No API configuration needed.
